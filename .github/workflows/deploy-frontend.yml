name: Deploy frontend

on:
    push:
        branches:
            - main
        paths:
            - ".github/workflows/**"
            - "packages/frontend/**"
            - "package.json"
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - run: corepack enable
            - uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Install dependencies
              run: yarn install --immutable

            - name: Build frontend
              run: yarn frontend:build

            - name: Tar build files
              run: |
                  tar -czf frontend.tar.gz package.json yarn.lock packages/frontend/.next packages/frontend/package.json

            - name: Copy to server
              uses: appleboy/scp-action@v1
              with:
                  host: ${{ secrets.DROPLET_HOST }}
                  username: ${{ secrets.DROPLET_USER }}
                  key: ${{ secrets.DROPLET_KEY }}
                  source: frontend.tar.gz
                  target: /tmp
                  overwrite: true

            - name: Extract and restart frontend process
              uses: appleboy/ssh-action@v1.0.3
              with:
                  host: ${{ secrets.DROPLET_HOST }}
                  username: ${{ secrets.DROPLET_USER }}
                  key: ${{ secrets.DROPLET_KEY }}
                  script: |
                      pm2 stop frontend

                      mkdir -p /var/www/solvely
                      tar -xzf /tmp/frontend.tar.gz -C /var/www/solvely
                      rm /tmp/frontend.tar.gz

                      cd /var/www/solvely
                      yarn install --immutable

                      pm2 start frontend
